import 'package:flutter/material.dart';

class ImageAureaHomePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Align(
        alignment: Alignment(-0, -0.8),
        child: Image.asset('image/aurea.png', height: 150,),
      );
  }
}